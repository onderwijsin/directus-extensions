---
'@onderwijsin/directus-magic-links-bundle': major
---

Remove the magic-links-specific and global locked-schema switches; schema setup is always lock-coordinated and uses the configured provider.
