---
'@onderwijsin/directus-coolify-deployments-bundle': minor
---

Add locked startup schema management for the `coolify_applications` collection through a new
`coolify-deployments-hook` bundle entry.
