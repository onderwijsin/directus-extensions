---
'@onderwijsin/directus-coolify-deployments-bundle': patch
---

Use Directus `PUBLIC_URL` for browser-origin validation behind reverse proxies and allow consumers
to disable the same-origin check explicitly.
