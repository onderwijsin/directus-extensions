---
'@onderwijsin/directus-magic-links-bundle': patch
---

Validate SMTP configuration, support configured collection names, refresh schema state between
Directus schema phases, and correct the magic-link user relation.
