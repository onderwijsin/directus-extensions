---
'@onderwijsin/directus-extension-utils': minor
'@onderwijsin/directus-sluggernaut-bundle': patch
---

Add the `withCache` helper with an optional namespace prefix configured through an options object,
and cache Sluggernaut field configuration reads for a configurable duration with collection-scoped
invalidation.
