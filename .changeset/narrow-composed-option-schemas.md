---
'@onderwijsin/directus-extension-utils': minor
---

Allow extension-owned option schemas to narrow included shared configuration keys after shared
parsing, defaults, and transforms. Rename the composition callback to `options` and support the
object form without `include`.
