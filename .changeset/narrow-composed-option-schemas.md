---
'@onderwijsin/directus-extension-utils': minor
---

Allow extension-owned option schemas to validate and narrow included shared configuration keys after
shared parsing, defaults, transforms, and cross-field checks while preserving the canonical shared
output. Rename the composition callback to `options` and support the object form without `include`.
