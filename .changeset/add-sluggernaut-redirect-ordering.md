---
'@onderwijsin/directus-sluggernaut-bundle': minor
---

Add backend-owned default ordering for redirect reads: exact redirects first, then patterns by descending specificity and stable `id` order.
