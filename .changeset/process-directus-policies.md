---
'@onderwijsin/directus-extension-utils': minor
---

Add validation and processing for nested Directus policy permissions, and make
`ensureDirectusPolicy` persist linked `directus_permissions` rows idempotently.
