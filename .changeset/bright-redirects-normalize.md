---
'@onderwijsin/directus-sluggernaut-bundle': minor
---

Treat exact redirect paths with and without trailing slashes as the same identity and add optional persisted redirect trailing-slash normalization.
