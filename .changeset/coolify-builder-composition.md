---
'@onderwijsin/directus-coolify-deployments-bundle': patch
---

Compose Coolify environment options with shared startup and cache configuration while preserving its enabled-by-default cache behavior.
