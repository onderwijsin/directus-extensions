---
'@onderwijsin/directus-extension-utils': minor
---

Add schema builders and declarative shared configuration fragments that supply the package-owned Zod
runtime. Multiple shared configurations can be composed with order-independent, identity-deduplicated
dependencies and explicit duplicate-key errors, while specialized builders, raw shared schema
exports, and consumer-owned raw Zod schemas remain supported.
