- [x] Suppose a document contains multiple references to the same item, and those references are
      outdated. The drawers of those references now corrently show a notice of that state. And if i
      refresh one of them via the drawer, that specific reference node is updated. But when i upon
      the drawer of the other reference, the notice + refresh button is also gone. Even though the
      data is still stale. Even when i do a hard refresh of the page, the notice is no longer
      showing on the stale reference drawer. Which is weird, becasue like wise behavioru DOES work
      via the reference report (eg multiple stale references to the same item are properly supported
      there)

- [x] The reference drawer now correctly shows a notice if the referenced item is outdated. But the
      other stale states need to be handled here as well!
