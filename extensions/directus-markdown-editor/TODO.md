- [ ] If i click an inline component, the drawer opens. When i close the drawer, the bubble action
      menu is displayed (as if text was selected)

- [ ] Can we make the reference report modal wider? like 75vw for > 768px screens?

- [ ] Decrease the font size in the reference report table. Like 2 or 3 px smaller.

- [ ] The reference report status should be renderer in a small VChip with an appropriate color

- [ ] The reference report success state has too much space between success title and description

- [ ] In reference item search, there is no space between the search input and the no results notice
      (and maybe this is true for other status messages)

- [ ] The slash menu always opens 'below' the current line, regardless of its position within the
      viewport. It would be better UX to determine popover menu alignment (top, bottom) based on the
      available space within the viewport. Eg, if the current line where '/' is triggered in near
      the bottom of the viewport, the context menu shoudl align to bottom, so that its visible
      (instead of out of view)
