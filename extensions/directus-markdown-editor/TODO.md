- [ ] The inline node view now has a nice warning color for archived items. Can we also add a status
      color for unavailable references (red / error color)
- [ ] In the reference report, the `archived` and `unavailable` status chips have a description
      below it (maybe other statuses have this as well). Can we move this description to a tooltip,
      so its not visible by default? And then make sure every status has a tooltip description /
      hint
