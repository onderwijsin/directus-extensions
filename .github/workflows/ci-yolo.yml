name: CI (YOLO)

on:
  pull_request:
    types: [labeled]

permissions:
  contents: read

jobs:
  ci:
    if: github.event.label.name == 'YOLO'
    uses: ./.github/workflows/ci.yml
    with:
      yolo: true
